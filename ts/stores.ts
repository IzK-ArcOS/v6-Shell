import { Store } from "$ts/writable";

export const StartMenuOpened = Store<boolean>(false);
export const ActionCenterOpened = Store<boolean>(false);
export const TrayIconOpened = Store<boolean>(false);
